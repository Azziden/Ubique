{% extends 'base.html.twig' %}

{% block title %}{{ magazine.codeAffaireEnClair }}{% endblock %}

{% block body %}
    <div class="container accueil-bg m-auto">

        {% if app.user %}

            {# <form method="get">
                <div class="form-group">
                    <input type="text" class="form-control" placeholder="Entrez nom du titre ou le nom du client" name="mots" value="{{ mots }}">
                </div>
                <div class="form-group mt-2">
                    <button class="btn btn-primary">Rechercher</button>
                </div>
            </form>#}

            <div class="container">
                <h1 class='text-center'>Détail {{ magazine.codeAffaireEnClair }}</h1>
                <table class="table table-striped">


                    <thead class="">
                    <tr>
                        <th scope="col">Nom</th>
                        <th scope="col">Statut</th>
                        <th scope="col">Total</th>
                    </tr>
                    </thead>
                    <tbody>
                    {% set totalCharged = 0 %}

                    {% for salarieEtEntreprise in salarie_et_entreprises %}
                        <tr>
                            <td>
                                {{ salarieEtEntreprise.nom_d_usage }}
                            </td>
                            <td>
                                {{ salarieEtEntreprise.statut }}
                            </td>
                            <td>
                                {% set totalCharged =  totalCharged + salarieEtEntreprise.total %}
                                {{ salarieEtEntreprise.total|format_currency('EUR')  }}
                            </td>
                        </tr>
                    {% endfor %}

                    <tr>
                        <td colspan="2"><strong>TOTAL</strong></td>
                        <td><strong>{{ totalCharged|format_currency('EUR') }}</strong></td>
                        <td colspan="2"></td>
                    </tr>
                    </tbody>
                </table>

            </div>
        {% endif %}

    </div>

{% endblock %}
