{# templates/registration/registration.html.twig #}{% extends 'base.html.twig' %}
{% block body %}
	<div class="container">
		<div class="row">
			<div class="col-md-10 ml-md-auto">
				<div class="card bg-light mb-3 mt-5" style="width:800px">
					<div class="card-body">
						<div class="card-header mb-3">S'inscrire
						</div>
						{{ form_start(form) }}
						{{ form(form) }}
							{{ form_end(form) }}
						</div>
					</div>
				</div>
			</div>
		</div>
	{% endblock %}
